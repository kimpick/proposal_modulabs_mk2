# 🤖 AI Agent 작업 가이드라인 (AGENT_GUIDE.md)

이 문서는 AI 어시스턴트가 본 프로젝트에서 제안 작업을 수행할 때 반드시 준수해야 하는 규칙입니다.

---

## 1. 사전 확인 절차
- 새로운 제안서 작성 또는 기존 제안서 수정 전, 반드시 아래 파일들을 읽어야 합니다.
  - `README.md`: 프로젝트 구조 및 빌드 방법 확인
  - `projects/<기업명>/context.md`: 해당 기업의 특수 상황 및 요구사항 확인
  - `schemas/`: 작성하려는 제안서 타입에 맞는 JSON 스키마 확인

## 2. 제안서 내용 작성 원칙
- **사실 관계**: `context.md`에 명시되지 않은 담당자 이름, 가격, 구체적 장소 등은 임의로 지어내지 않습니다. (필요 시 `[미정]`, `[협의 필요]` 등으로 표기)
- **톤앤매너**: 모두의연구소의 브랜드 이미지에 맞게 전문적이면서도 혁신적인 톤을 유지합니다.
- **도구 선정**: 기업의 인프라 환경(`context.md` 확인)에 따라 사용 가능한 도구만 제안합니다. (예: 폐쇄망 환경에서는 설치형/로컬 구동 도구 우선 고려)
- **일관성**: `templates/base.css`에 정의된 컬러 팔레트(#EE3E4C 등)와 일치하는 용어를 사용합니다.

## 3. 기술적 제약 사항
- `content.json`의 키 이름(steps, tracks, modules, seminars 등)은 스키마에 정의된 대로만 사용합니다. `seminars` 키는 K-HP 사업 전용(khp-seminar.html)이며, 대학·일반 기업 세미나는 `tracks` 키를 사용합니다.
- HTML 태그 사용 시 `<ul>`, `<li>`, `<b>`, `<br>` 등 렌더링 가능한 기본 태그만 사용합니다.
- 빌드는 반드시 `build.py` 스크립트를 통해서만 수행하며, 개별 `build_pdf.py`를 새로 만들지 않습니다.

## 4. 워크플로우 준수
- AI는 새로운 제안 작업을 요청받으면 반드시 `.agent/workflows/proposal-generation.md`에 정의된 단계를 따릅니다.
