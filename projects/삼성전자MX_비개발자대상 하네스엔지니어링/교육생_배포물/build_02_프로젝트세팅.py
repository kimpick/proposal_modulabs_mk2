# -*- coding: utf-8 -*-
"""02. 하네스 기본 프로젝트 세팅 — 교육 전 환경 점검 (소요 20분)"""
import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))
import _kit as K

OUT = os.path.join(os.path.dirname(os.path.abspath(__file__)),
                   '[모두의연구소]하네스_교육생용_02_하네스_기본_프로젝트_세팅.docx')

d = K.new_doc()
K.doc_frame(d,
            '삼성전자 MX사업부 · 비개발직군 하네스 엔지니어링 파일럿 · 교육생 배포물 ②',
            '하네스 기본 프로젝트 세팅')

K.title_block(
    d,
    '하네스 기본 프로젝트 세팅',
    '교육 전 환경 점검 · 20분 · MX-harness 폴더(folder, 폴더) 사용',
    '교육일 2026년 8월 21일(금) 예정          점검자 ______________          점검일 ____ 월 ____ 일'
)

K.box(d, [
    '**확인할 것은 두 가지입니다. 앱이 내 폴더의 파일을 읽을 수 있는지, 그 폴더에 파일을 만들 수 있는지.**',
    '이 두 가지가 되면 당일 실습은 전부 돌아갑니다. 안 되면 첫 교시에서만 20분 넘게 쓰게 되고 오후 일정이 밀립니다. '
    '그래서 교육 전에 한 번 돌려 봅니다.',
    '일곱 단계, 모두 20분입니다. 순서대로 따라 하시면 됩니다.',
], accent=K.RED, fill=K.PINK, border=K.PINK_B)

# ── 준비물 ────────────────────────────────────────────────
K.h2(d, '0', '시작 전에 세 가지를 확인합니다')
K.table(d, ['준비물', '어떤 상태여야 하나', '없거나 안 될 때'],
        [['Claude 데스크톱 앱\n(클로드 데스크톱 앱)',
          '설치와 로그인이 끝난 상태',
          '교육 담당자에게 알려 주십시오.\n앱과 계정은 회사에서 준비합니다'],
         ['MX-harness-kit.zip\n(엠엑스 하네스 키트)',
          '따로 받으신 압축 파일',
          '교육 담당자에게 다시 보내 달라고 하십시오'],
         ['파일 탐색기\n(File Explorer, 파일 탐색기)',
          '윈도우에 원래 있는 기능',
          '5단계에서 파일이 실제로 생겼는지\n눈으로 확인할 때 씁니다']],
        [24, 30, 46])
K.para(d, '앱 설치나 로그인이 안 되어 있으면 1단계부터 진행되지 않습니다. 그럴 때는 여기서 멈추고 담당자에게 알리는 것이 가장 빠릅니다.',
       color=K.MUTED, before=3)

# ── 1단계 ────────────────────────────────────────────────
K.h2(d, '1단계', '폴더를 제자리에 두기      (3분)')
K.bullets(d, [
    '받으신 `MX-harness-kit.zip` 을 풀면 `MX-harness` 폴더가 나옵니다.',
    '이 폴더를 **폴더째로** **문서** 폴더 안에 둡니다. 안에 있는 파일만 따로 옮기지 않습니다.',
    '자리 예) `C:\\Users\\<사번>\\Documents\\MX-harness`',
])
K.para(d, '폴더를 열어 아래가 보이면 제대로 놓인 것입니다.', before=5, after=3)
K.table(d, ['안에 있는 것', '무엇인가'],
        [['_check.md', '4단계 읽기 확인에 쓰는 파일'],
         ['README 파일', '키트 사용법을 적어 둔 안내문'],
         ['knowledge', '앱이 파일을 만들어 넣을 자리. 5단계에서 씁니다'],
         ['source · input · output · skills', '당일 실습에 쓸 폴더. 지금 비어 있는 것이 정상입니다']],
        [30, 70])
K.box(d, [
    '**폴더 이름은 바꾸지 마십시오.** 당일 화면에 나오는 이름과 달라지면 서른 명이 동시에 헤맵니다.',
    '바탕화면이나 다운로드 폴더에 두어도 돌아가기는 합니다. 다만 자리가 달라지면 문제가 생겼을 때 찾기 어렵습니다.',
], fill=K.SOFT2)

# ── 2단계 ────────────────────────────────────────────────
K.h2(d, '2단계', '앱에서 Code 탭 열기      (2분)')
K.para(d, '앱에는 탭(tab, 탭)이 세 개 있습니다. 내 PC의 파일을 읽고 쓰는 것은 **Code 탭에서만** 됩니다. '
          '다른 탭에서 물으면 파일을 못 읽고 일반적인 답만 옵니다.')
K.table(d, ['탭', '무엇을 하는 곳인가', '오늘 쓰나'],
        [['Chat (챗)', '대화만 주고받는 곳', '쓰지 않습니다'],
         ['Cowork (코워크)', '문서 작업을 같이 하는 곳', '쓰지 않습니다'],
         ['Code (코드)', '내 PC의 폴더를 연결해 파일을 읽고 만드는 곳', '**이 탭만 씁니다**']],
        [20, 56, 24])

# ── 3단계 ────────────────────────────────────────────────
K.h2(d, '3단계', '작업 폴더 연결하기      (3분)')
K.bullets(d, [
    'Code 탭에서 폴더를 고르는 항목을 찾아 `MX-harness` 폴더를 고릅니다.',
    '“이 폴더에 접근해도 되나요” 같은 창이 뜨면 **허용**을 누릅니다. 허용하지 않으면 파일을 읽지 못합니다.',
    '연결되면 파일 목록에 `_check.md` 가 보입니다. 여기까지 오면 절반은 끝났습니다.',
])

# ── 4단계 ────────────────────────────────────────────────
K.h2(d, '4단계', '읽기 확인      (3분)')
K.para(d, '아래 문장을 그대로 입력해 보십시오. 고쳐 쓰지 않아도 됩니다.')
K.prompt_box(d, ['_check.md 파일 내용을 읽어줘'])
K.para(d, '**통과 기준** — 답에 `환경 준비 완료` 라는 문장이 나오면 통과입니다.', before=5)
K.para(d, '나오지 않으면 뒤쪽 표의 A, B를 보십시오.', color=K.MUTED, after=7)

# ── 5단계 ────────────────────────────────────────────────
K.h2(d, '5단계', '쓰기 확인 — 가장 중요한 단계      (6분)')
K.para(d, '이어서 아래 문장을 그대로 입력합니다.')
K.prompt_box(d, ['knowledge 폴더에 test-node.md 파일을 만들고,',
                 '맨 위에 title과 type을 넣어줘'])
K.box(d, [
    '**Accept(액셉트, 승인) 버튼이 뜨면 반드시 누르십시오.**',
    '앱은 파일을 만들거나 고치기 전에 매번 승인을 받습니다. 이 버튼을 누르지 않으면 앱이 “만들었습니다”라고 답해도 파일은 없습니다. '
    '당일 가장 많이 누를 버튼입니다.',
], accent=K.RED, fill=K.PINK, border=K.PINK_B)

K.h3(d, '통과 기준 — 두 가지를 눈으로 봅니다')
K.table(d, ['확인할 것', '어떻게 확인하나'],
        [['① 파일이 실제로 있다',
          '**파일 탐색기로** `MX-harness\\knowledge` 폴더를 직접 열어 `test-node.md` 가 있는지 봅니다.\n'
          '앱의 답만 믿지 않습니다. 답과 실제가 다른 경우가 있습니다'],
         ['② 파일 안에 정보가 들어 있다',
          '그 파일을 메모장으로 열어, 맨 위에 `---` 로 감싼 부분이 있고 그 안에 `title` 과 `type` 이 있는지 봅니다.\n'
          '예)  `title: 테스트`  ·  `type: 점검`']],
        [26, 74])
K.para(d, '두 가지가 확인되면 `test-node.md` 는 **지웁니다.** 점검용으로 만든 파일이라 당일에는 필요하지 않습니다.', before=5)

# ── 6단계 ────────────────────────────────────────────────
K.h2(d, '6단계', '폴더 다섯 개의 쓰임 알아 두기      (2분)')
K.para(d, '한 번만 읽어 두시면 당일 설명이 짧아집니다. 지금은 다섯 폴더가 모두 비어 있는 것이 정상입니다.')
K.table(d, ['폴더', '무엇을 담는 곳인가', '예'],
        [['knowledge', '정리를 마친 문서를 모아 두는 곳', '쪼개서 정리한 회의록'],
         ['source', '아직 정리하지 않은 원본을 두는 곳', '3주치가 섞인 회의록 원본'],
         ['input', '자동화에 넣을 파일을 두는 곳', '이번 주 메모'],
         ['output', '자동화가 만들어 낸 파일이 저장되는 곳', '주간보고 초안'],
         ['skills', '되풀이하는 일을 명령 하나로 만들어 두는 곳', '“주간보고 만들기” 명령']],
        [20, 48, 32])

# ── 7단계 ────────────────────────────────────────────────
K.h2(d, '7단계', '완료 확인      (1분)')
K.checklist(d, [
    '**문서** 폴더 안에 `MX-harness` 폴더가 있다',
    'Code 탭에서 그 폴더가 연결되고 파일 목록이 보인다',
    '읽기 확인에서 `환경 준비 완료` 문장을 받았다',
    '쓰기 확인에서 `test-node.md` 가 실제로 생긴 것을 탐색기로 보고 지웠다',
])
K.box(d, [
    '네 개 모두 되면 준비 완료입니다.',
    '하나라도 안 되면 다음 장 표를 먼저 보십시오. 그래도 안 되면 **몇 단계에서 막혔는지 번호로** '
    '교육 담당자에게 알려 주십시오. 당일 준비에 반영합니다.',
], fill=K.SOFT2)

# ── 증상표 ────────────────────────────────────────────────
K.h2(d, '부록 1', '막혔을 때 보는 표')
K.table(d, ['', '이런 증상', '왜 그런가', '이렇게 하십시오'],
        [['A', '파일 내용은 안 나오고\n일반적인 답만 옵니다',
          '폴더가 연결되지 않았거나\nCode 탭이 아닙니다',
          'Code 탭인지 보고 폴더를 다시 연결합니다.\n물을 때 파일 이름을 직접 적습니다'],
         ['B', '`환경 준비 완료` 가\n안 나옵니다',
          '`_check.md` 가 다른 자리에\n있습니다',
          '`_check.md` 가 `MX-harness` 폴더 **바로 아래**에 있는지 봅니다'],
         ['C', '“만들었습니다”라는데\n파일이 없습니다',
          'Accept 버튼을 안 눌렀습니다',
          '다시 시키고 화면에 뜨는 승인 버튼을 누릅니다.\n가장 자주 나오는 증상입니다'],
         ['D', '허용 창이 안 뜨거나\n폴더를 못 고릅니다',
          '앱 권한이나 계정 정책\n때문입니다',
          '앱을 완전히 닫고 다시 엽니다.\n그래도 안 되면 담당자에게 알립니다'],
         ['E', '탭에 Code 가 없습니다',
          '앱 버전이 다릅니다',
          '담당자에게 버전 확인을 요청합니다.\n서른 대가 같은 버전이어야 당일 안내가 맞습니다'],
         ['F', '로그인이 안 됩니다',
          '계정이 아직 없거나\n사내망 문제입니다',
          '개인 계정으로 돌아가지 마시고 담당자에게 알립니다'],
         ['G', '어제는 됐는데\n오늘은 안 됩니다',
          '폴더 연결이 풀렸습니다',
          '폴더를 다시 연결하고 허용합니다.\n앱을 새로 열면 자주 생깁니다'],
         ['H', '탐색기에서 파일이\n안 보입니다',
          '**문서** 폴더가 클라우드로\n동기화되는 중일 수 있습니다',
          '동기화가 끝난 뒤 다시 봅니다.\n그래도 안 보이면 폴더를 `C:\\MX-harness` 로 옮겨 다시 연결합니다'],
         ['I', '답이 중간에 끊깁니다',
          '한 번에 시킨 양이 많습니다',
          '파일 하나씩 나눠 시킵니다.\n한 번에 여러 폴더를 다루지 않습니다']],
        [5, 22, 25, 48], size=9.5, head_size=9, key_col=0)

# ── 부록 2 ────────────────────────────────────────────────
K.h2(d, '부록 2', '다른 앱도 함께 점검하는 경우 (선택)')
K.para(d, '당일 실습은 Claude 데스크톱 앱 하나로 합니다. 다만 사내 PC에서는 앱마다 폴더를 다루는 방식이 조금 다릅니다. '
          '다른 앱을 이미 쓰고 계시면 미리 확인해 두면 당일에 빠릅니다.')
K.bullets(d, [
    'Codex(코덱스) · Antigravity(안티그래비티) · Gemini(제미나이) 순서로 위 **4단계와 5단계만** 똑같이 해 봅니다.',
    '앱을 바꾸기 전에 `knowledge` 폴더의 `test-node.md` 를 지웁니다. 남겨 두면 다음 앱이 파일을 새로 만들지 않아 확인이 되지 않습니다.',
    '되는 앱과 안 되는 앱을 아래에 적어 두시면 됩니다.',
])
K.table(d, ['앱', '읽기 확인', '쓰기 확인', '메모'],
        [['Claude (필수)', '☐ 통과   ☐ 실패', '☐ 통과   ☐ 실패', ''],
         ['Codex', '☐ 통과   ☐ 실패', '☐ 통과   ☐ 실패', ''],
         ['Antigravity', '☐ 통과   ☐ 실패', '☐ 통과   ☐ 실패', ''],
         ['Gemini', '☐ 통과   ☐ 실패', '☐ 통과   ☐ 실패', '']],
        [22, 22, 22, 34], zebra=False)
K.para(d, '앱이 설치되어 있지 않으면 이 부록은 넘어가셔도 됩니다. 명령창으로 쓰는 도구를 설치해야 하는 분은 함께 받으신 '
          '「Windows 환경 설치 안내서」를 보십시오.', color=K.MUTED, before=4)

K.footer_note(d, [
    '이 점검은 당일 1교시의 환경 확인 시간을 실습으로 돌리기 위한 것입니다.',
    '점검 결과와 막힌 단계 번호를 교육 담당자에게 알려 주시면 당일 준비에 반영합니다.',
])
K.brand_footer(d, '모두의연구소 기업교육팀')

d.save(OUT)
print('saved:', OUT)
